---
name: "Release"

on:
  workflow_dispatch:
  release:

jobs:
  release:
    name: "Release"
    runs-on: ubuntu-20.04
    steps:
      - name: 'Checkout'
        uses: actions/checkout@v2

      - name: 'Prerequisites'
        run: .\scripts\Prerequisites.ps1 -ApiKey ${{ secrets.PWSH_GALLERY }}
        shell: pwsh