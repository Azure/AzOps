name: "Release"

on:
  workflow_dispatch:
  release:
    types: [created]

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Publish
      run: |
        Publish-Module -Path './src/' -NuGetApiKey ''